# ksc_metaverse — Claude Code

The shared rule set for this repository is `AGENTS.md` — Codex (the other agent, on another PC) reads it directly and it is imported here verbatim. Change rules there, never here.

@AGENTS.md

## Claude Only

- Nothing here may restate or contradict `AGENTS.md`. If a rule applies to both agents, it belongs in `AGENTS.md`.
