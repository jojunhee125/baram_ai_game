# 미결 우려사항 — 세션 시작 시 먼저 읽을 것

> 작성 2026-09-23, 기준 커밋 `9325f4c`. 코드 구조·로드맵·git 이력을 읽기 전용으로 점검한 결과다.
> 3번은 **사용자가 해결을 확정한 필수 과제**다. 나머지(1·2·4)는 사용자 판단 대기이며, 판단 전에 어느 PC·어느 에이전트도 임의로 정리하지 않는다.
> 항목이 해결되면 해결한 커밋에서 이 파일의 해당 항목을 지우고 `decisions.md`에 한 줄 남긴다. 모두 해결되면 파일을 삭제하고 `CLAUDE.md`·`AGENTS.md`·`roadmap.md`의 안내 줄도 함께 지운다.
> 커밋 단위(크기·묶음)는 우려 대상이 아니다 — 커밋은 자유롭게 한다(사용자 확정 2026-09-23).

우선순위: **3(필수) → 4 → 1 → 2**

## 1. `metaverseRoom.ts` 비대화 — 상태: 판단 대기 (긴급 아님)

- 사실: `server/src/rooms/metaverseRoom.ts` 3,953줄, `onMessage` 핸들러 25개, 메서드 약 65개, import 33개. 증가분 대부분은 R03~R05(2026-09-16~18, 커밋당 +176~+406줄)다. 파티·거래·제작은 `partySystem.ts`·`tradeSystem.ts`·`craftingSystem.ts`로 분리됐고 room에는 배선 +105줄만 들어왔다.
- 문제: 로직보다 **배선 집중**이 문제다. 이 파일에 조용히 깨지는 불변식이 있다 — grand-plaza(500 CCU 기준선) 무영향, `refreshViewsAround`·`handleChat` 청중 루프 본문 수정 금지, `hasMonsters` 게이트. 기능이 늘수록 접촉 빈도가 올라간다.
- 판단할 것: R10(문파·PvP) 착수 전에 기능별 시스템으로 분할할지, 그대로 둘지.

## 2. 검증 테스트 파일 중복·옛 명칭 — 상태: 판단 대기 (낮음)

- 사실: 테스트 101개 중 18개가 옛 Phase·검증 명칭이다. 보스는 `passI-boss-verification`(50케이스)·`passI-boss-independent-reverification`(22)·`passI-boss-capacity`(3)로 3분할, `armorEquip-*`도 2벌. "새로운 검증자가 재검증" 방식이 매번 새 파일을 만든 결과다.
- 문제: 버그는 아니다. 한 동작을 바꾸면 2~3개 파일을 함께 고쳐야 하고 어느 파일이 정본인지 불명확하다.
- 판단할 것: 기능 단위 파일로 통합할지, 보류할지.

## 3. 두 에이전트가 서로 다른 문서 규칙을 따름 — 상태: **반드시 해결 (최우선, 심각)**

> **사용자 확정(2026-09-23): 아래 규칙 전환을 사용자는 지시한 적이 없다. 문서 규칙은 반드시 하나로 통일한다.**
> 두 PC(Claude·Codex)가 각자 다른 규칙으로 문서를 쓰면 서로의 결과를 계속 뒤집는다. 이것은 선택 사항이 아니라 반드시 해결할 결함이다.

```
2026-09-17 규칙 (CLAUDE.md "Historical Markdown Discipline")
  로드맵 항목당 md 1개(r<NN>-*.md), 날짜별 implementation-*.md 금지
        ↓  9560690 (2026-09-23, 다른 PC)
CLAUDE.md 5~7행 추가: "AGENTS.md 우선, 작업마다 implementation 기록 필수"
  → 기존 규칙을 Historical로 격하
        ↓  결과
docs/ 날짜별 design·implementation 파일 21개(이틀), roadmap.md 37KB·상단 공지 10개
```

- 공지마다 테스트 수가 다르다(1175·1193·1203·1249·1322). 무엇이 최신 기준인지 독자가 추론해야 한다.
- 해결 방향: `9560690`의 규칙 전환은 **무승인 변경이므로 무효**다. 통일 기준은 2026-09-17 규칙(로드맵 항목당 `r<NN>-*.md` 1개, 날짜별 파일 금지)이다.
- 해야 할 일:
  1. `CLAUDE.md` 7행("AGENTS.md 우선, 작업마다 implementation 기록 필수")을 제거하고 Historical 절을 다시 현행 규칙으로 올린다. 5행의 날짜별 파일 참조도 정리한다.
  2. `AGENTS.md`에 같은 규칙을 명시해 Codex도 동일하게 따르게 한다(두 파일이 같은 규칙을 말해야 한다).
  3. 날짜별 `design-/implementation-2026-*.md` 21개를 해당 `r<NN>-*.md`로 통합하고 원본 삭제 + 참조 수정을 같은 커밋에서 한다.
  4. `roadmap.md` 상단 날짜별 공지를 현재 상태 요약 하나로 합친다.
- 통일 작업이 끝나기 전에는 어느 PC도 날짜별 문서를 **새로 만들지 않는다.**

## 4. 보안 재판단 트리거 충족, 재판단 흔적 없음 — 상태: 판단 대기

- 사실: 기존 보류 결정 — 게이트웨이 신원 헤더를 메시지별 검증 없이 신뢰(Medium). 재판단 트리거는 "아이템을 소모·삭제·**이전**하는 엔드포인트(사용·상점·거래)가 처음 생길 때". 상점(R04-c, 2026-09-18)과 개인 거래(G, 2026-09-23)가 이미 존재한다. `implementation-2026-09-23-social-economy.md`에는 "Guardian blocker 0"만 있고 헤더 신뢰 재판단 언급이 없으며, APISIX SSO header/cookie PoC는 미실행으로 기록돼 있다.
- 문제: 거래 이전에는 신원 위조가 "남에게 증정"에 그쳤지만, 거래가 생기면서 "남의 아이템을 가져오는" 경로가 생겼다. 단 "Traefik ForwardAuth가 외부 주입 헤더를 삭제 후 재설정한다"는 전제가 유지되면 실제 악용은 불가하고, 사내 전용이라 허용도도 높다.
- 판단할 것: 거래·상점 경로의 신원 출처만 좁게 보안 재점검할지, 보류할지.

## 점검 결과 문제 아님

- `4ae0045`의 사냥 콘텐츠 교체는 기존 몬스터 키(squirrel·rabbit·deer·boss)를 유지하고 키만 추가했다. `itemDefinitions.ts` 변경은 +5줄로 고정 키·순서 규칙 위반은 보이지 않았다.
